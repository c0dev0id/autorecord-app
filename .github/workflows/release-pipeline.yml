name: Release Pipeline

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Release version (e.g., v1.0.0) - leave empty to auto-increment patch version'
        required: false
        type: string

# Only one release workflow at a time
concurrency:
  group: release-pipeline
  cancel-in-progress: false

jobs:
  # Job 1: Warm caches
  warm-caches:
    name: Warm Gradle Caches
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Cache Gradle Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/**') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Create gradle.properties
        run: |
          echo "GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON=${{ toJSON(secrets.GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON) }}" >> gradle.properties
          echo "OSM_CLIENT_ID=${{ secrets.OSM_CLIENT_ID }}" >> gradle.properties
          echo "org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8" >> gradle.properties
          echo "android.useAndroidX=true" >> gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties
          echo "kotlin.code.style=official" >> gradle.properties
          echo "org.gradle.caching=true" >> gradle.properties
          echo "org.gradle.parallel=true" >> gradle.properties
          echo "org.gradle.daemon=false" >> gradle.properties
          echo "org.gradle.configureondemand=true" >> gradle.properties

      - name: Warm up Gradle cache
        run: ./gradlew tasks --no-daemon --build-cache

  # Job 2: Build release APK (optionally signed)
  build:
    name: Build Release APK
    runs-on: ubuntu-latest
    needs: warm-caches
    timeout-minutes: 60
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Restore Gradle Dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/**') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Create gradle.properties
        run: |
          echo "GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON=${{ toJSON(secrets.GOOGLE_CLOUD_SERVICE_ACCOUNT_JSON) }}" >> gradle.properties
          echo "OSM_CLIENT_ID=${{ secrets.OSM_CLIENT_ID }}" >> gradle.properties
          echo "org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8" >> gradle.properties
          echo "android.useAndroidX=true" >> gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties
          echo "kotlin.code.style=official" >> gradle.properties
          echo "org.gradle.caching=true" >> gradle.properties
          echo "org.gradle.parallel=true" >> gradle.properties
          echo "org.gradle.daemon=false" >> gradle.properties
          echo "org.gradle.configureondemand=true" >> gradle.properties

      # Optional: Setup signing if secrets are available
      - name: Setup Signing (Optional)
        # Avoid using secrets in the step-level expression to prevent linter issues;
        # instead provide them as env vars and handle checks in the shell.
        env:
          SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
          SIGNING_KEY_ALIAS:    ${{ secrets.SIGNING_KEY_ALIAS }}
          SIGNING_KEYSTORE_BASE64: ${{ secrets.SIGNING_KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail
      
          if [ -n "${SIGNING_KEY_PASSWORD:-}" ]; then
            echo "storeFile=../release.keystore" > keystore.properties
            echo "storePassword=${SIGNING_KEY_PASSWORD}" >> keystore.properties
            echo "keyAlias=${SIGNING_KEY_ALIAS:-release}" >> keystore.properties
            echo "keyPassword=${SIGNING_KEY_PASSWORD}" >> keystore.properties
      
            # If keystore provided as base64 secret, decode it
            if [ -n "${SIGNING_KEYSTORE_BASE64:-}" ]; then
              echo "$SIGNING_KEYSTORE_BASE64" | base64 -d > ../release.keystore
            fi
      
            echo "Signing configuration written to keystore.properties"
          else
            echo "Signing secrets not provided; skipping signing setup."
          fi
      
            - name: Build Release APK
              run: |
                ./gradlew assembleRelease --stacktrace --build-cache --parallel
      
            - name: Upload Release APKs
              uses: actions/upload-artifact@v4
              with:
                name: app-release-apk
                path: |
                  app/build/outputs/apk/release/*.apk
                  vnmanager-opener/build/outputs/apk/release/*.apk
                retention-days: 90
      
            - name: Build Summary
              run: |
                echo "## 📦 Release Build Summary" >> $GITHUB_STEP_SUMMARY
                echo "" >> $GITHUB_STEP_SUMMARY
                echo "✅ Release APKs built successfully!" >> $GITHUB_STEP_SUMMARY
                echo "" >> $GITHUB_STEP_SUMMARY
                
                # Check Voice Notes app APK
                echo "### Voice Notes App" >> $GITHUB_STEP_SUMMARY
                APK_FILE=$(find app/build/outputs/apk/release -name "*.apk" | head -1)
                if [ -f "$APK_FILE" ]; then
                  APK_SIZE=$(stat -c%s "$APK_FILE" 2>/dev/null || stat -f%z "$APK_FILE")
                  APK_SIZE_MB=$(echo "scale=2; $APK_SIZE / 1024 / 1024" | bc)
                  echo "**APK Size:** ${APK_SIZE_MB} MB" >> $GITHUB_STEP_SUMMARY
                  
                  if [[ "$APK_FILE" == *"unsigned"* ]]; then
                    echo "**Status:** Unsigned (signing not configured)" >> $GITHUB_STEP_SUMMARY
                  else
                    echo "**Status:** Signed" >> $GITHUB_STEP_SUMMARY
                  fi
                fi
                
                # Check VN Manager Opener APK
                echo "" >> $GITHUB_STEP_SUMMARY
                echo "### VN Manager Opener" >> $GITHUB_STEP_SUMMARY
                OPENER_APK=$(find vnmanager-opener/build/outputs/apk/release -name "*.apk" | head -1)
                if [ -f "$OPENER_APK" ]; then
                  OPENER_SIZE=$(stat -c%s "$OPENER_APK" 2>/dev/null || stat -f%z "$OPENER_APK")
                  OPENER_SIZE_MB=$(echo "scale=2; $OPENER_SIZE / 1024 / 1024" | bc)
                  echo "**APK Size:** ${OPENER_SIZE_MB} MB" >> $GITHUB_STEP_SUMMARY
                  
                  if [[ "$OPENER_APK" == *"unsigned"* ]]; then
                    echo "**Status:** Unsigned (signing not configured)" >> $GITHUB_STEP_SUMMARY
                  else
                    echo "**Status:** Signed" >> $GITHUB_STEP_SUMMARY
                  fi
                fi

  # Job 3: Run critical smoke tests (optional Firebase Test Lab)
  smoke-tests:
    name: Critical Smoke Tests
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 60
    if: github.event_name == 'workflow_dispatch'  # Only run on manual dispatch
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Release APK
        uses: actions/download-artifact@v4
        with:
          name: app-release-apk
          path: apks/

      - name: Run Local Smoke Test
        run: |
          echo "Running local smoke tests..."
          echo "✅ APK smoke test passed (placeholder)"
          # Add actual smoke test logic here if needed

  # Job 4: Upload release artifacts - creates GitHub Release
  upload-release-artifacts:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 15
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Release APK
        uses: actions/download-artifact@v4
        with:
          name: app-release-apk
          path: apks/

      - name: Determine Release Tag
        id: release_info
        env:
          DISPATCH_TAG: ${{ github.event.inputs.release_version }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail

          # 1) Prefer explicit workflow_dispatch input
          if [ -n "${DISPATCH_TAG:-}" ]; then
            TAG="${DISPATCH_TAG}"
          # 2) If run was triggered by a tag push, use the pushed tag
          elif [ -n "${GITHUB_REF_NAME:-}" ]; then
            TAG="${GITHUB_REF_NAME}"
          else
            # 3) Otherwise compute next patch from latest semver tag
            git fetch --tags --quiet
            LATEST_TAG=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname | head -n1 || true)

            if [ -z "${LATEST_TAG}" ]; then
              # No tags yet, start at v0.0.1
              TAG="v0.0.1"
            else
              # Strip leading v if present and split into parts
              BASE=${LATEST_TAG#v}
              IFS='.' read -r MAJOR MINOR PATCH <<< "${BASE}"
              # Ensure PATCH is numeric; safe fallback
              if ! printf '%s' "${PATCH}" | grep -Eq '^[0-9]+$'; then
                PATCH=0
              fi
              PATCH=$((PATCH + 1))
              TAG="v${MAJOR}.${MINOR}.${PATCH}"
            fi
          fi

          echo "tag=${TAG}" > "$GITHUB_OUTPUT"
          echo "Release tag: ${TAG}"

          if printf '%s' "${TAG}" | grep -Eq '^v[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "is_release=true" > "$GITHUB_OUTPUT"
          else
            echo "is_release=false" > "$GITHUB_OUTPUT"
          fi

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.release_info.outputs.tag }}
          release_name: Release ${{ steps.release_info.outputs.tag }}
          draft: ${{ steps.release_info.outputs.is_release == 'false' }}
          prerelease: ${{ steps.release_info.outputs.is_release == 'false' }}
          body: |
            ## Release ${{ steps.release_info.outputs.tag }}
            
            ### Changes
            - See CHANGELOG.md for details
            
            ### APK Files
            This release includes two Android applications:
            
            **1. Voice Notes App** (`voice-notes-release.apk`)
            - Main motorcycle voice notes recording application
            - Package: com.voicenotes.motorcycle
            
            **2. VN Manager Opener** (`vnmanager-opener-release.apk`)
            - Companion utility for managing voice notes
            - Package: com.vnmanager.opener
            
            Download the appropriate APK file(s) below. For signed releases, you can install directly. For unsigned releases, you'll need to sign before installing.

      - name: Find and Prepare APK Files
        id: find_apks
        run: |
          # Find Voice Notes app APK
          APP_APK=$(find apks/app/build/outputs/apk/release -name "*.apk" 2>/dev/null | head -1)
          if [ -n "$APP_APK" ]; then
            echo "app_apk_path=$APP_APK" >> $GITHUB_OUTPUT
            echo "app_apk_name=voice-notes-release.apk" >> $GITHUB_OUTPUT
            echo "Found Voice Notes APK: $APP_APK"
          else
            echo "Warning: Voice Notes APK not found!"
          fi
          
          # Find VN Manager Opener APK
          OPENER_APK=$(find apks/vnmanager-opener/build/outputs/apk/release -name "*.apk" 2>/dev/null | head -1)
          if [ -n "$OPENER_APK" ]; then
            echo "opener_apk_path=$OPENER_APK" >> $GITHUB_OUTPUT
            echo "opener_apk_name=vnmanager-opener-release.apk" >> $GITHUB_OUTPUT
            echo "Found VN Manager Opener APK: $OPENER_APK"
          else
            echo "Warning: VN Manager Opener APK not found!"
          fi

      - name: Upload Voice Notes Release APK
        if: steps.find_apks.outputs.app_apk_path != ''
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ steps.find_apks.outputs.app_apk_path }}
          asset_name: ${{ steps.find_apks.outputs.app_apk_name }}
          asset_content_type: application/vnd.android.package-archive

      - name: Upload VN Manager Opener Release APK
        if: steps.find_apks.outputs.opener_apk_path != ''
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ steps.find_apks.outputs.opener_apk_path }}
          asset_name: ${{ steps.find_apks.outputs.opener_apk_name }}
          asset_content_type: application/vnd.android.package-archive

  # Job 5: Publish to Play Store (OPTIONAL - commented out by default)
  # Uncomment and configure this job to enable automatic Play Store publishing
  # publish-play-store:
  #   name: Publish to Google Play Store
  #   runs-on: ubuntu-latest
  #   needs: [build, smoke-tests]
  #   timeout-minutes: 30
  #   if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Download Release APK
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: app-release-apk
  #         path: apks/
  #
  #     - name: Publish to Play Store
  #       uses: r0adkll/upload-google-play@v1
  #       with:
  #         serviceAccountJsonPlainText: ${{ secrets.PLAY_STORE_SERVICE_ACCOUNT_JSON }}
  #         packageName: com.voicenotes.motorcycle
  #         releaseFiles: apks/app-release.apk
  #         track: production
  #         status: completed
  #         whatsNewDirectory: docs/whatsnew/
  #         mappingFile: app/build/outputs/mapping/release/mapping.txt

  # Final status summary
  release-summary:
    name: Release Summary
    runs-on: ubuntu-latest
    needs: [build, upload-release-artifacts]
    if: always()
    timeout-minutes: 5
    
    steps:
      - name: Generate Release Summary
        run: |
          echo "## 🚀 Release Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Build: ${{ needs.build.result }}" >> $GITHUB_STEP_SUMMARY
          echo "Upload: ${{ needs.upload-release-artifacts.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [[ "${{ needs.build.result }}" == "success" ]] && [[ "${{ needs.upload-release-artifacts.result }}" == "success" ]]; then
            echo "✅ Release completed successfully!" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Release pipeline failed!" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
